(package "replace-pairs" "0.1" "Find-replace pairs of things")

(files "replace-pairs.el")

;; Tell Cask to add package information to this file (so that we can have a
;; single file package).
(package-file "replace-pairs.el")

(source melpa-stable)

(development
 (depends-on "ecukes")
 (depends-on "espuds"))
